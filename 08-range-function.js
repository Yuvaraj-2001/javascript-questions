
const range = (start, end) => {
    return [...Array(end - start).keys()].map((el)=> el + start);
}

const rangeFor = () => {
    const range = [];
    for(let i = start; i <= end; i++){
        result.push(i)
    }
    return result;
}